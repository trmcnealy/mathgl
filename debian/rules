#!/usr/bin/make -f
  
include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

# Add here any variable or target overrides you need.
#DEB_CONFIGURE_EXTRA_FLAGS = --enable-tiff --enable-jpeg --enable-hdf5 --enable-glut --enable-fltk
DEB_CONFIGURE_EXTRA_FLAGS = --enable-all CXXFLAGS="-O2" CFLAGS="-O2"
binary-install/libmgl-dev::
	rm -f debian/libmgl-dev/usr/include/mgl/mgl_fltk.h
	rm -f debian/libmgl-dev/usr/include/mgl/mgl_glut.h
