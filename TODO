					MathGL (for v.1.11.*)

a. Colorbar: print more digits for manual arrays ... but avoid overlapping ???
0. Miss MAJOR labels or decrease its fontsize if labels too close.
	Also check that enough digits is printed to distingush the labels
	Also allow "time"-labels
	Also allow rotate labels if they too dense
2. Contour label should not rotated on ~180 degree
3. Help about 't'/'T' style for contours (+MGL)
4. Add MGL commands "function 'name'", "return [Res]" and modify "call 'name' [Res]"
7. FAQ about pthread and MathGL -- for mglGraphFLTK.
8. Keep text formating for next symbol -- for example '\b{b}none'
9. Bug with mult-line text in axis labels -- wrong shifting
10. Save glyph then it is used for mglGraphPS -- allow different font family in EPS|SVG ??!
11. Add small-caps in TeX parsing -- also do 2-wchar string parsing (text + attributes).
12. Import/Export via GIF (as 3d data) ??? + 16bit PNG ?!!
14. Options "iI_-aA" for colorbar
15. Hankel transform
16. The same function for ticks of axis and colorbar
17. Save id of subplots and axis for getting value of click coordinates arbitrary inplot -- fill only part which is overlapped. Clear it at Clf() + update for any Rotate, Aspect, InPlot, SetRanges, 
18. Add option ':[xyzd]' in color scheme for coloring along coordinate (default 'z') ?!?
19. Example of "ring" chart
20. Add sample for color scheme "UBbcgyqrR"
21. Regroup pictures in WWW by big classes + combo plots
22. Save ALL plot styles for each curve (0...N) for each subplot (0...M) in format \leg{N_M}
23. Add TeX command \leg{plot style} for line+mark of specified style.
24. Allow legend and colorbar in absolute coordinates ?!! the same as in Text() :)
25. Allow color for text if it is not text style
26. Add "colorbar x y ['sch'='' w=1 h=1]" + "colorbar x y dat ['sch'='' w=1 h=1]"
27. Rename 'stl'->'style', 'sch'->'scheme' in MGL docs ???
28. Add '[' as separator in string
29. INCOMPATIBLE CHANGES!!! swap color and font style in text option ('bR:r' -> 'r:bR')
30. INCOMPATIBLE CHANGES!!! hide 'dir' argument as ':[xyz]' for cont3, dens3, contf3, grid3 + remove conta, densa, grida, contfa
31. INCOMPATIBLE!!! Allow slice definition as real: sl<1 ? sl=int(n*sl) : int(sl);
32. INCOMPATIBLE CHANGES!!! tune colors as {} -- for example, 'b{3}'
33. INCOMPATIBLE CHANGES!!! hide unused arguments (like "size")
34. Export to X3D
35. Samples about gradient filling in Area() and Range; + About 'a' and 'f' options of Bars(), Barh() + 2 colors
36. Legend in OpenGL mode
37. Check if widget functions can be added to mgl_w.h !?! -- use #define ??!
38. Load GZ compressed files VFM -- add functions for handling "textual" streams like double stof(char **sptr).
39. Surface by interpolation for arbitrary points {x,y,z}.
40. Bug with 'a' option of text after Rotate ?!
41. Update STIX fonts -- new release 
42. Create PWT fonts
43. Allow interpolation in MGL if argument is not integer.
44. FAQ about cmake for mingw
45. Check HDF4 reading -- put in Cmake + configure + docs
46. Docs about SetLegendMarks/mgl_set_legend_marks
48. Docs about Grad(), TriCont(), QuadPlot()
49. Inplace arrays as [v1,v2,v3] in MGL


1. Plot for isolines of vector fields ???
2. Named arguments in MGL ???
3. \dfrac for normal size and centering ???
4. "Chaos" in area.png (near axis) ???
5. save data coordinates in picture??? -- NO -- slow + a lot of memory!

					MathGL (for v.2.?) ???

1. GTK window ???
11. Use Hershey as built-in font ???

					FAQ

1. On which things the subplot influence
2. About unrotated text
3. Add "Howto" section in web site.

					Web
					
1. "Data plotting", "Data plot", "Scientific visualtization", "Surface", "Cloud", 
	"Plot", "statistical graphics"
2. Examples of animation
3. Example of inverted axis, minus-log axis
